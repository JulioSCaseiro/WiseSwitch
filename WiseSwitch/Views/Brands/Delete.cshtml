@model WiseSwitch.Data.Entities.Brand

@{
    ViewData["Title"] = "Delete brand";
}

<h2><partial name="_GoBackFaIcon" /> Delete Brand</h2>

<div>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Name)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Name)
        </dd>
    </dl>

    <hr />

    @if (ViewBag.IsDeletable)
    {
        <p>Are you sure you want to delete this Brand?</p>

        <form asp-action="Delete">
            <button type="submit" value="@Model.Id" class="btn btn-danger">Delete</button>
        </form>
    }
    else
    {
        <p>You can't delete this Brand because it has at least one Product Line registered in the database.</p>
        <ul>
            @foreach (var item in ViewBag.ProductLinesNames)
            {
                <li>@item</li>
            }
        </ul>
    }

</div>
