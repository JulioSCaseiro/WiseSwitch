@model IEnumerable<WiseSwitch.ViewModels.Entities.SwitchModel.IndexRowSwitchModelViewModel>

@{
    ViewData["Title"] = "Switch Models";
}

<link rel="stylesheet" href="https://cdn.datatables.net/1.10.25/css/dataTables.bootstrap4.min.css" />

<div class="row">
    <div class="col-md-12">
        <div class="panel panel-default">
            <div class="panel-heading">
                <h1 class="panel-title">Switch Models</h1>
                <p>
                    <a asp-action="Create" class="btn btn-success"><i class="fa fa-square-plus"></i> New Switch Model</a>
                </p>
            </div>
            <div class="panel-body">
                <table class="table table-hover" id="MyTable">
                    <thead>
                        <tr>
                            @* <th><!-- # --></th> *@
                            <th>
                                @Html.DisplayNameFor(model => model.BrandName)
                            </th>
                            <th>
                                @Html.DisplayNameFor(model => model.ProductLineName)
                            </th>
                            <th>
                                @Html.DisplayNameFor(model => model.ProductSeriesName)
                            </th>
                            <th>
                                @Html.DisplayNameFor(model => model.ModelName)
                            </th>
                            <th>
                                @Html.DisplayNameFor(model => model.ModelYear)
                            </th>
                            <th>
                                @Html.DisplayNameFor(model => model.DefaultFirmwareVersion)
                            </th>
                            <th><!-- actions --></th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in Model)
                        {
                            <tr>
                                @* <td>#</td> *@
                                <td>
                                    @Html.DisplayFor(modelItem => item.BrandName)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.ProductLineName)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.ProductSeriesName)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.ModelName)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.ModelYear)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.DefaultFirmwareVersion)
                                </td>
                                <td>
                                    <a asp-action="Edit" asp-route-id="@item.Id" class="btn btn-primary"><i class="fa fa-edit"></i></a>
                                    <a asp-action="Delete" asp-route-id="@item.Id" class="btn btn-danger"><i class="fa fa-trash"></i></a>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>

@section Scripts{
    @{
        await Html.RenderPartialAsync("Scripts/_DataTablesScriptsPartial");
    }
}